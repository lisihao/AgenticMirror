# AgenticMirror V2 架构设计说明书

> 版本: 2.0.0
> 创建日期: 2024-01-17
> 状态: 设计完成，待实现
> 基于: V1 基线 + 优化建议

---

## 目录

1. [版本概述](#1-版本概述)
2. [V2 新增功能](#2-v2-新增功能)
3. [系统架构升级](#3-系统架构升级)
4. [页面设计](#4-页面设计)
5. [组件库设计](#5-组件库设计)
6. [数据模型扩展](#6-数据模型扩展)
7. [状态管理设计](#7-状态管理设计)
8. [主题系统设计](#8-主题系统设计)
9. [电商闭环设计](#9-电商闭环设计)
10. [AI 能力增强](#10-ai-能力增强)
11. [实现计划](#11-实现计划)
12. [测试策略](#12-测试策略)

---

## 1. 版本概述

### 1.1 V2 目标

V2 版本在 V1 基础上实现以下核心目标：

| 目标 | 描述 | 优先级 |
|------|------|--------|
| **电商闭环** | 完整的购物车→结算→订单流程 | P0 |
| **主题统一** | 暗/亮模式一键切换 | P0 |
| **产品详情** | 完整的产品信息展示 | P0 |
| **教程完善** | 教程详情页实现 | P0 |
| **组件库** | 可复用组件提取 | P1 |
| **状态持久化** | LocalStorage + Context | P1 |
| **收藏系统** | 产品/妆容/教程收藏 | P1 |
| **AI 聊天** | 自然语言交互 | P2 |

### 1.2 版本对比

| 维度 | V1 | V2 |
|------|-----|-----|
| 页面数量 | 12 | 16 (+4) |
| 组件复用率 | 40% | 80% |
| 电商完整度 | 60% | 95% |
| 主题支持 | 部分 | 完整 |
| 状态持久化 | 无 | 有 |
| AI 交互 | 展示型 | 交互型 |

---

## 2. V2 新增功能

### 2.1 新增页面

| 页面 | 路由 | 功能描述 |
|------|------|----------|
| 购物车 | /demo/cart | 购物车管理、优惠计算 |
| 产品详情 | /demo/product/[id] | 产品完整信息、评价、推荐 |
| 订单确认 | /demo/checkout | 地址、支付、确认 |
| 订单列表 | /demo/orders | 历史订单、状态追踪 |

### 2.2 新增功能模块

```
V2 新增功能
├── 电商闭环
│   ├── 购物车（添加/删除/数量调整）
│   ├── 优惠券系统
│   ├── 地址管理
│   ├── 订单确认
│   └── 订单追踪
├── 用户系统
│   ├── 收藏夹
│   ├── 浏览历史
│   └── 偏好设置
├── 主题系统
│   ├── 亮色模式
│   ├── 暗色模式
│   └── 跟随系统
├── AI 增强
│   ├── 聊天面板
│   ├── Agent 协作可视化
│   └── AR 试妆预览（占位）
└── 组件库
    ├── 基础组件
    ├── 业务组件
    └── 图表组件
```

---

## 3. 系统架构升级

### 3.1 V2 架构图

```
┌─────────────────────────────────────────────────────────────────────┐
│                         用户界面层 (Next.js 14)                       │
├─────────────────────────────────────────────────────────────────────┤
│ Dashboard │ Mirror │ Analysis │ Recommendations │ Styling │ Commerce │
│ Inventory │ Tutorials │ Workflow │ Companion │ Cart │ Product │ Orders │
├─────────────────────────────────────────────────────────────────────┤
│                         组件库层 (Component Library)                   │
├────────────┬─────────────┬─────────────┬────────────────────────────┤
│  基础组件   │   业务组件   │   图表组件   │        布局组件            │
│  Button    │  ProductCard │  LineChart  │      DemoLayout           │
│  Card      │  CartItem    │  PieChart   │      DemoSidebar          │
│  Badge     │  OrderCard   │  BarChart   │      ThemeProvider        │
│  Input     │  TutorialCard│  Progress   │      ChatPanel            │
│  Modal     │  StyleCard   │  Gauge      │                           │
├────────────┴─────────────┴─────────────┴────────────────────────────┤
│                         状态管理层 (State Management)                  │
├─────────────────────────────────────────────────────────────────────┤
│         React Context │ useReducer │ LocalStorage Persist           │
├─────────────────────────────────────────────────────────────────────┤
│  CartContext │ ThemeContext │ FavoritesContext │ UserContext        │
├─────────────────────────────────────────────────────────────────────┤
│                         数据层 (Data Layer)                           │
├─────────────────────────────────────────────────────────────────────┤
│    Mock Data │ Type Definitions │ API Contracts │ Validators        │
├─────────────────────────────────────────────────────────────────────┤
│                         工具层 (Utilities)                            │
├─────────────────────────────────────────────────────────────────────┤
│    cn() │ formatPrice() │ formatDate() │ storage │ validators       │
└─────────────────────────────────────────────────────────────────────┘
```

### 3.2 目录结构升级

```
web/frontend/
├── app/
│   └── demo/
│       ├── layout.tsx
│       ├── dashboard/page.tsx
│       ├── mirror/page.tsx
│       ├── analysis/page.tsx
│       ├── recommendations/page.tsx
│       ├── styling/page.tsx
│       ├── commerce/page.tsx
│       ├── inventory/page.tsx
│       ├── tutorials/
│       │   ├── page.tsx
│       │   └── [id]/page.tsx        # V2 完善
│       ├── workflow/page.tsx
│       ├── companion/page.tsx
│       ├── competition/page.tsx
│       ├── investment/page.tsx
│       ├── cart/page.tsx             # V2 新增
│       ├── product/[id]/page.tsx     # V2 新增
│       ├── checkout/page.tsx         # V2 新增
│       └── orders/page.tsx           # V2 新增
├── components/
│   ├── ui/                           # V2 新增：基础组件库
│   │   ├── Button.tsx
│   │   ├── Card.tsx
│   │   ├── Badge.tsx
│   │   ├── Input.tsx
│   │   ├── Modal.tsx
│   │   ├── Toast.tsx
│   │   ├── Skeleton.tsx
│   │   └── index.ts
│   ├── business/                     # V2 新增：业务组件
│   │   ├── ProductCard.tsx
│   │   ├── CartItem.tsx
│   │   ├── OrderCard.tsx
│   │   ├── TutorialCard.tsx
│   │   ├── StyleCard.tsx
│   │   └── index.ts
│   ├── charts/                       # V2 新增：图表组件
│   │   ├── LineChart.tsx
│   │   ├── PieChart.tsx
│   │   ├── GaugeChart.tsx
│   │   └── index.ts
│   ├── layout/
│   │   ├── DemoSidebar.tsx
│   │   ├── Sidebar.tsx
│   │   ├── ThemeToggle.tsx           # V2 新增
│   │   └── ChatPanel.tsx             # V2 新增
│   └── workflow/
│       └── ...existing...
├── contexts/                         # V2 新增：状态管理
│   ├── CartContext.tsx
│   ├── ThemeContext.tsx
│   ├── FavoritesContext.tsx
│   ├── UserContext.tsx
│   └── index.ts
├── hooks/                            # V2 新增：自定义 Hooks
│   ├── useCart.ts
│   ├── useTheme.ts
│   ├── useFavorites.ts
│   ├── useLocalStorage.ts
│   └── index.ts
├── lib/
│   ├── types/                        # V2 扩展：类型定义
│   │   ├── user.ts
│   │   ├── product.ts
│   │   ├── order.ts                  # V2 新增
│   │   ├── cart.ts                   # V2 新增
│   │   └── index.ts
│   ├── constants/
│   │   ├── mockData.ts
│   │   ├── theme.ts                  # V2 新增
│   │   └── index.ts
│   └── utils/
│       ├── cn.ts
│       ├── format.ts                 # V2 新增
│       ├── storage.ts                # V2 新增
│       └── index.ts
└── styles/
    ├── globals.css
    └── themes/                       # V2 新增
        ├── light.css
        └── dark.css
```

---

## 4. 页面设计

### 4.1 购物车页面 (/demo/cart)

```
┌─────────────────────────────────────────────────────────────┐
│  购物车 (3)                                    [继续购物]    │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  ┌──────────────────────────────────────────────────────┐   │
│  │ [x] [图片] 产品名称                    数量[-][2][+]  │   │
│  │     品牌 · ¥299                        小计: ¥598     │   │
│  │     [删除] [移入收藏]                                 │   │
│  └──────────────────────────────────────────────────────┘   │
│                                                              │
│  ┌──────────────────────────────────────────────────────┐   │
│  │ [x] [图片] 产品名称                    数量[-][1][+]  │   │
│  │     品牌 · ¥459                        小计: ¥459     │   │
│  └──────────────────────────────────────────────────────┘   │
│                                                              │
├─────────────────────────────────────────────────────────────┤
│  右侧：结算栏                                                │
│  ┌──────────────────────┐                                   │
│  │  商品总计    ¥1,057  │                                   │
│  │  运费        ¥0      │                                   │
│  │  优惠券      -¥50    │  [输入优惠码]                     │
│  │  ──────────────────  │                                   │
│  │  应付总计    ¥1,007  │                                   │
│  │                      │                                   │
│  │  [去结算 (2件)]      │                                   │
│  └──────────────────────┘                                   │
│                                                              │
│  推荐搭配（基于购物车商品）                                  │
│  [产品卡片] [产品卡片] [产品卡片]                            │
└─────────────────────────────────────────────────────────────┘
```

### 4.2 产品详情页 (/demo/product/[id])

```
┌─────────────────────────────────────────────────────────────┐
│  < 返回                              [收藏] [分享]           │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  ┌──────────────┐  产品名称                                 │
│  │              │  品牌                                     │
│  │   大图展示    │  ★★★★☆ 4.8 (2,847评价)                  │
│  │              │                                           │
│  │  [1] [2] [3] │  ¥299  ¥399                              │
│  └──────────────┘                                           │
│                                                              │
│  规格选择                                                    │
│  容量: [30ml] [50ml] [100ml]                                │
│  色号: [#01] [#02] [#03]                                    │
│                                                              │
│  AI 匹配分析                                                 │
│  ┌──────────────────────────────────────────────────────┐   │
│  │ 🎯 肤色匹配 96%  👤 肤质适合 92%  ⭐ 综合推荐 94%     │   │
│  │ AI 说：这款产品非常适合您的暖色调肌肤，建议搭配...    │   │
│  └──────────────────────────────────────────────────────┘   │
│                                                              │
│  [加入购物车]  [立即购买]                                    │
│                                                              │
├─────────────────────────────────────────────────────────────┤
│  Tab: [商品详情] [成分表] [用户评价] [使用教程]             │
│                                                              │
│  商品详情内容...                                             │
│                                                              │
├─────────────────────────────────────────────────────────────┤
│  相似推荐                                                    │
│  [产品卡片] [产品卡片] [产品卡片] [产品卡片]                │
└─────────────────────────────────────────────────────────────┘
```

### 4.3 订单确认页 (/demo/checkout)

```
┌─────────────────────────────────────────────────────────────┐
│  确认订单                                                    │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  收货地址                                    [管理地址]      │
│  ┌──────────────────────────────────────────────────────┐   │
│  │ ● 张小姐 138****8888                                  │   │
│  │   上海市浦东新区陆家嘴环路1000号                      │   │
│  └──────────────────────────────────────────────────────┘   │
│                                                              │
│  商品清单                                                    │
│  ┌──────────────────────────────────────────────────────┐   │
│  │ [图片] 产品名称 x2                              ¥598  │   │
│  │ [图片] 产品名称 x1                              ¥459  │   │
│  └──────────────────────────────────────────────────────┘   │
│                                                              │
│  配送方式                                                    │
│  ● 标准配送（免费）预计 1-3 天                              │
│  ○ 次日达（+¥15）                                          │
│                                                              │
│  支付方式                                                    │
│  ● 支付宝  ○ 微信支付  ○ 银行卡                            │
│                                                              │
├─────────────────────────────────────────────────────────────┤
│  订单备注: [请输入备注...]                                   │
├─────────────────────────────────────────────────────────────┤
│  商品总计    ¥1,057                                         │
│  运费        ¥0                                              │
│  优惠        -¥50                                            │
│  ──────────────────────────────────────                     │
│  应付总计    ¥1,007                                         │
│                                                              │
│  [提交订单]                                                  │
└─────────────────────────────────────────────────────────────┘
```

### 4.4 订单列表页 (/demo/orders)

```
┌─────────────────────────────────────────────────────────────┐
│  我的订单                                                    │
├─────────────────────────────────────────────────────────────┤
│  Tab: [全部] [待付款] [待发货] [待收货] [已完成]             │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  ┌──────────────────────────────────────────────────────┐   │
│  │ 订单号: 2024011700001        2024-01-17 14:30         │   │
│  │ ─────────────────────────────────────────────────     │   │
│  │ [图片] 产品名称 x2  [图片] 产品名称 x1                │   │
│  │                                                       │   │
│  │ 共 3 件商品  合计: ¥1,007                            │   │
│  │                                                       │   │
│  │ 状态: 已发货 - 预计明天送达          [查看物流]       │   │
│  │                                      [确认收货]       │   │
│  └──────────────────────────────────────────────────────┘   │
│                                                              │
│  ┌──────────────────────────────────────────────────────┐   │
│  │ 订单号: 2024011600002        2024-01-16 10:15         │   │
│  │ ─────────────────────────────────────────────────     │   │
│  │ [图片] 产品名称 x1                                    │   │
│  │                                                       │   │
│  │ 共 1 件商品  合计: ¥299                              │   │
│  │                                                       │   │
│  │ 状态: 已完成                         [再次购买]       │   │
│  │                                      [评价商品]       │   │
│  └──────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────┘
```

### 4.5 教程详情页 (/demo/tutorials/[id])

```
┌─────────────────────────────────────────────────────────────┐
│  < 返回教程列表                            [收藏] [分享]     │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  ┌──────────────────────────────────────────────────────┐   │
│  │                                                       │   │
│  │                   视频播放区域                        │   │
│  │                   (带进度条)                          │   │
│  │                                                       │   │
│  └──────────────────────────────────────────────────────┘   │
│                                                              │
│  教程标题                                                    │
│  ★★★☆☆ 难度  ⏱ 15分钟  👁 12,345次观看                     │
│                                                              │
│  步骤列表（可点击跳转）                                       │
│  ┌──────────────────────────────────────────────────────┐   │
│  │ ● Step 1: 妆前打底 (0:00-2:30)                       │   │
│  │   使用妆前乳均匀涂抹全脸                              │   │
│  │   💡 AI 提示：向上提拉手法更有效                      │   │
│  │   产品：兰蔻妆前乳 [查看产品]                         │   │
│  ├──────────────────────────────────────────────────────┤   │
│  │ ○ Step 2: 底妆上妆 (2:30-5:00)                       │   │
│  │   使用粉底液点涂后推开                                │   │
│  │   💡 AI 提示：用美妆蛋轻拍更自然                      │   │
│  │   产品：雅诗兰黛 DW [查看产品]                        │   │
│  ├──────────────────────────────────────────────────────┤   │
│  │ ○ Step 3: 眼妆 (5:00-10:00)                          │   │
│  │ ○ Step 4: 唇妆 (10:00-12:00)                         │   │
│  │ ○ Step 5: 定妆 (12:00-15:00)                         │   │
│  └──────────────────────────────────────────────────────┘   │
│                                                              │
│  使用产品                                                    │
│  [产品卡片] [产品卡片] [产品卡片]         [一键加入购物车]   │
│                                                              │
│  相关教程                                                    │
│  [教程卡片] [教程卡片] [教程卡片]                            │
└─────────────────────────────────────────────────────────────┘
```

---

## 5. 组件库设计

### 5.1 基础组件 (ui/)

#### Button

```typescript
interface ButtonProps {
  variant: 'primary' | 'secondary' | 'outline' | 'ghost' | 'danger';
  size: 'sm' | 'md' | 'lg';
  loading?: boolean;
  disabled?: boolean;
  fullWidth?: boolean;
  icon?: ReactNode;
  children: ReactNode;
  onClick?: () => void;
}
```

#### Card

```typescript
interface CardProps {
  variant: 'default' | 'elevated' | 'outlined';
  padding: 'none' | 'sm' | 'md' | 'lg';
  hoverable?: boolean;
  clickable?: boolean;
  children: ReactNode;
  onClick?: () => void;
}
```

#### Badge

```typescript
interface BadgeProps {
  variant: 'default' | 'success' | 'warning' | 'danger' | 'info';
  size: 'sm' | 'md';
  dot?: boolean;
  children?: ReactNode;
}
```

#### Modal

```typescript
interface ModalProps {
  open: boolean;
  onClose: () => void;
  title?: string;
  size: 'sm' | 'md' | 'lg' | 'full';
  closeOnOverlayClick?: boolean;
  children: ReactNode;
  footer?: ReactNode;
}
```

#### Toast

```typescript
interface ToastProps {
  type: 'success' | 'error' | 'warning' | 'info';
  message: string;
  duration?: number;
  action?: {
    label: string;
    onClick: () => void;
  };
}

// 使用方式
toast.success('添加成功');
toast.error('操作失败');
```

#### Skeleton

```typescript
interface SkeletonProps {
  variant: 'text' | 'circular' | 'rectangular';
  width?: number | string;
  height?: number | string;
  animation: 'pulse' | 'wave' | 'none';
}
```

### 5.2 业务组件 (business/)

#### ProductCard

```typescript
interface ProductCardProps {
  product: Product;
  variant: 'grid' | 'list' | 'compact';
  showAIReason?: boolean;
  showMatchScore?: boolean;
  onAddToCart?: () => void;
  onFavorite?: () => void;
  onClick?: () => void;
}
```

#### CartItem

```typescript
interface CartItemProps {
  item: CartItem;
  onQuantityChange: (quantity: number) => void;
  onRemove: () => void;
  onMoveToFavorites: () => void;
}
```

#### OrderCard

```typescript
interface OrderCardProps {
  order: Order;
  variant: 'list' | 'detail';
  onViewDetail?: () => void;
  onTrackShipping?: () => void;
  onReorder?: () => void;
}
```

---

## 6. 数据模型扩展

### 6.1 购物车模型

```typescript
// lib/types/cart.ts

export interface CartItem {
  id: string;
  productId: string;
  product: Product;
  quantity: number;
  selectedSku?: {
    size?: string;
    color?: string;
    variant?: string;
  };
  addedAt: string;
}

export interface Cart {
  id: string;
  userId: string;
  items: CartItem[];
  createdAt: string;
  updatedAt: string;
}

export interface CartSummary {
  itemCount: number;
  subtotal: number;
  shipping: number;
  discount: number;
  total: number;
  appliedCoupon?: Coupon;
}

export interface Coupon {
  code: string;
  type: 'percentage' | 'fixed';
  value: number;
  minAmount?: number;
  expiresAt: string;
}
```

### 6.2 订单模型

```typescript
// lib/types/order.ts

export interface Order {
  id: string;
  orderNumber: string;
  userId: string;
  items: OrderItem[];
  status: OrderStatus;
  shippingAddress: Address;
  paymentMethod: PaymentMethod;
  pricing: OrderPricing;
  tracking?: TrackingInfo;
  createdAt: string;
  updatedAt: string;
}

export interface OrderItem {
  productId: string;
  productName: string;
  productImage: string;
  brand: string;
  quantity: number;
  unitPrice: number;
  totalPrice: number;
  sku?: string;
}

export type OrderStatus =
  | 'pending_payment'
  | 'paid'
  | 'processing'
  | 'shipped'
  | 'delivered'
  | 'completed'
  | 'cancelled'
  | 'refunded';

export interface Address {
  id: string;
  name: string;
  phone: string;
  province: string;
  city: string;
  district: string;
  street: string;
  postalCode?: string;
  isDefault: boolean;
}

export type PaymentMethod = 'alipay' | 'wechat' | 'card' | 'balance';

export interface OrderPricing {
  subtotal: number;
  shipping: number;
  discount: number;
  couponDiscount: number;
  total: number;
}

export interface TrackingInfo {
  carrier: string;
  trackingNumber: string;
  status: string;
  estimatedDelivery: string;
  history: TrackingEvent[];
}

export interface TrackingEvent {
  time: string;
  location: string;
  description: string;
}
```

### 6.3 收藏模型

```typescript
// lib/types/favorites.ts

export interface FavoriteItem {
  id: string;
  type: 'product' | 'style' | 'tutorial';
  itemId: string;
  addedAt: string;
}

export interface FavoriteProduct extends FavoriteItem {
  type: 'product';
  product: Product;
}

export interface FavoriteStyle extends FavoriteItem {
  type: 'style';
  style: MakeupStyle;
}

export interface FavoriteTutorial extends FavoriteItem {
  type: 'tutorial';
  tutorial: Tutorial;
}
```

---

## 7. 状态管理设计

### 7.1 Context 结构

```typescript
// contexts/CartContext.tsx

interface CartState {
  items: CartItem[];
  loading: boolean;
  error: string | null;
}

interface CartActions {
  addItem: (product: Product, quantity?: number) => void;
  removeItem: (itemId: string) => void;
  updateQuantity: (itemId: string, quantity: number) => void;
  clearCart: () => void;
  applyCoupon: (code: string) => Promise<boolean>;
}

type CartContextType = CartState & CartActions;
```

```typescript
// contexts/ThemeContext.tsx

interface ThemeState {
  theme: 'light' | 'dark' | 'system';
  resolvedTheme: 'light' | 'dark';
}

interface ThemeActions {
  setTheme: (theme: 'light' | 'dark' | 'system') => void;
  toggleTheme: () => void;
}
```

```typescript
// contexts/FavoritesContext.tsx

interface FavoritesState {
  items: FavoriteItem[];
  loading: boolean;
}

interface FavoritesActions {
  addFavorite: (type: string, itemId: string) => void;
  removeFavorite: (id: string) => void;
  isFavorite: (type: string, itemId: string) => boolean;
}
```

### 7.2 LocalStorage 持久化

```typescript
// hooks/useLocalStorage.ts

function useLocalStorage<T>(
  key: string,
  initialValue: T
): [T, (value: T | ((val: T) => T)) => void] {
  // 实现存储持久化
}

// 使用示例
const [cart, setCart] = useLocalStorage<CartItem[]>('cart', []);
const [theme, setTheme] = useLocalStorage<string>('theme', 'system');
const [favorites, setFavorites] = useLocalStorage<FavoriteItem[]>('favorites', []);
```

---

## 8. 主题系统设计

### 8.1 CSS 变量定义

```css
/* styles/themes/light.css */
:root {
  /* 品牌色 */
  --color-primary: #E91E63;
  --color-primary-light: #FCE4EC;
  --color-primary-dark: #C2185B;

  /* 背景色 */
  --color-bg-primary: #FFFFFF;
  --color-bg-secondary: #F5F5F5;
  --color-bg-tertiary: #EEEEEE;

  /* 文字色 */
  --color-text-primary: #212121;
  --color-text-secondary: #757575;
  --color-text-tertiary: #9E9E9E;

  /* 边框色 */
  --color-border: #E0E0E0;
  --color-border-light: #F0F0F0;

  /* 状态色 */
  --color-success: #4CAF50;
  --color-warning: #FF9800;
  --color-error: #F44336;
  --color-info: #2196F3;

  /* 阴影 */
  --shadow-sm: 0 1px 2px rgba(0,0,0,0.05);
  --shadow-md: 0 4px 6px rgba(0,0,0,0.1);
  --shadow-lg: 0 10px 15px rgba(0,0,0,0.1);
}

/* styles/themes/dark.css */
[data-theme="dark"] {
  --color-bg-primary: #121212;
  --color-bg-secondary: #1E1E1E;
  --color-bg-tertiary: #2C2C2C;

  --color-text-primary: #FFFFFF;
  --color-text-secondary: #B0B0B0;
  --color-text-tertiary: #808080;

  --color-border: #333333;
  --color-border-light: #404040;

  --shadow-sm: 0 1px 2px rgba(0,0,0,0.3);
  --shadow-md: 0 4px 6px rgba(0,0,0,0.4);
  --shadow-lg: 0 10px 15px rgba(0,0,0,0.5);
}
```

### 8.2 主题切换组件

```typescript
// components/layout/ThemeToggle.tsx

export function ThemeToggle() {
  const { theme, setTheme } = useTheme();

  return (
    <button onClick={() => setTheme(theme === 'light' ? 'dark' : 'light')}>
      {theme === 'light' ? <Moon /> : <Sun />}
    </button>
  );
}
```

---

## 9. 电商闭环设计

### 9.1 购物流程

```
浏览商品 → 加入购物车 → 查看购物车 → 去结算 → 确认订单 → 支付 → 订单完成
    │           │            │           │          │        │
    ▼           ▼            ▼           ▼          ▼        ▼
  收藏      数量调整      优惠券      选地址     选支付    订单追踪
```

### 9.2 购物车逻辑

```typescript
// 添加商品
const addToCart = (product: Product, quantity: number = 1) => {
  const existingItem = cart.find(item => item.productId === product.id);
  if (existingItem) {
    updateQuantity(existingItem.id, existingItem.quantity + quantity);
  } else {
    const newItem: CartItem = {
      id: generateId(),
      productId: product.id,
      product,
      quantity,
      addedAt: new Date().toISOString(),
    };
    setCart([...cart, newItem]);
  }
  toast.success('已加入购物车');
};

// 计算总价
const calculateSummary = (): CartSummary => {
  const subtotal = cart.reduce(
    (sum, item) => sum + item.product.price * item.quantity,
    0
  );
  const shipping = subtotal >= 299 ? 0 : 15;
  const discount = appliedCoupon ? calculateDiscount(subtotal, appliedCoupon) : 0;
  return {
    itemCount: cart.reduce((sum, item) => sum + item.quantity, 0),
    subtotal,
    shipping,
    discount,
    total: subtotal + shipping - discount,
    appliedCoupon,
  };
};
```

### 9.3 订单流程

```typescript
// 创建订单
const createOrder = async (checkoutData: CheckoutData): Promise<Order> => {
  const order: Order = {
    id: generateId(),
    orderNumber: generateOrderNumber(),
    userId: user.id,
    items: cart.map(cartItemToOrderItem),
    status: 'pending_payment',
    shippingAddress: checkoutData.address,
    paymentMethod: checkoutData.paymentMethod,
    pricing: calculatePricing(cart, checkoutData),
    createdAt: new Date().toISOString(),
    updatedAt: new Date().toISOString(),
  };

  // 清空购物车
  clearCart();

  // 模拟支付
  await simulatePayment(order);

  return order;
};
```

---

## 10. AI 能力增强

### 10.1 聊天面板设计

```typescript
// components/layout/ChatPanel.tsx

interface ChatMessage {
  id: string;
  role: 'user' | 'assistant';
  content: string;
  timestamp: string;
  suggestions?: string[];
}

interface ChatPanelProps {
  isOpen: boolean;
  onClose: () => void;
}

// 功能：
// - 自然语言查询产品
// - 妆容推荐对话
// - 穿搭建议对话
// - 皮肤问题咨询
```

### 10.2 Agent 协作可视化

```
┌─────────────────────────────────────────────────────────────┐
│  AI Agent 协作流程                                           │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  [用户输入]                                                  │
│       ↓                                                      │
│  ┌─────────┐    ┌─────────┐    ┌─────────┐    ┌─────────┐   │
│  │ Agent 1 │───▶│ Agent 2 │───▶│ Agent 3 │───▶│ Agent 4 │   │
│  │ 趋势分析│    │ 肤色匹配│    │ 场景适配│    │ 价格优化│   │
│  └─────────┘    └─────────┘    └─────────┘    └─────────┘   │
│       ↓              ↓              ↓              ↓         │
│  ┌──────────────────────────────────────────────────────┐   │
│  │                    结果汇总                           │   │
│  │  综合推荐列表 + 决策理由 + 置信度                     │   │
│  └──────────────────────────────────────────────────────┘   │
│                                                              │
└─────────────────────────────────────────────────────────────┘
```

---

## 11. 实现计划

### 11.1 Phase 1: 基础设施 (Day 1)

| 任务 | 优先级 | 预计工时 |
|------|--------|----------|
| 创建组件库目录结构 | P0 | 0.5h |
| 实现基础 UI 组件 | P0 | 2h |
| 实现 Context 状态管理 | P0 | 1.5h |
| 实现主题系统 | P0 | 1h |
| 实现 LocalStorage Hook | P0 | 0.5h |

### 11.2 Phase 2: 电商闭环 (Day 1-2)

| 任务 | 优先级 | 预计工时 |
|------|--------|----------|
| 实现购物车页面 | P0 | 2h |
| 实现产品详情页 | P0 | 2h |
| 实现订单确认页 | P0 | 1.5h |
| 实现订单列表页 | P0 | 1.5h |
| 集成购物车 Context | P0 | 1h |

### 11.3 Phase 3: 功能完善 (Day 2)

| 任务 | 优先级 | 预计工时 |
|------|--------|----------|
| 实现教程详情页 | P0 | 2h |
| 实现收藏系统 | P1 | 1.5h |
| 统一主题切换 | P0 | 1h |
| 组件复用重构 | P1 | 2h |

### 11.4 Phase 4: 测试验证 (Day 2)

| 任务 | 优先级 | 预计工时 |
|------|--------|----------|
| 功能测试 | P0 | 1h |
| 主题测试 | P0 | 0.5h |
| 购物流程测试 | P0 | 0.5h |
| Bug 修复 | P0 | 1h |

---

## 12. 测试策略

### 12.1 功能测试清单

- [ ] 购物车
  - [ ] 添加商品
  - [ ] 修改数量
  - [ ] 删除商品
  - [ ] 应用优惠券
  - [ ] 计算总价正确

- [ ] 产品详情
  - [ ] 图片展示
  - [ ] SKU 选择
  - [ ] 加入购物车
  - [ ] AI 匹配展示

- [ ] 订单流程
  - [ ] 地址选择
  - [ ] 支付方式选择
  - [ ] 订单创建
  - [ ] 订单状态显示

- [ ] 主题切换
  - [ ] 亮色模式正常
  - [ ] 暗色模式正常
  - [ ] 持久化正确

- [ ] 收藏系统
  - [ ] 添加收藏
  - [ ] 移除收藏
  - [ ] 收藏状态显示
  - [ ] 持久化正确

### 12.2 页面访问测试

```bash
# 所有页面应正常访问
/demo/dashboard
/demo/mirror
/demo/analysis
/demo/recommendations
/demo/styling
/demo/commerce
/demo/inventory
/demo/tutorials
/demo/tutorials/1
/demo/workflow
/demo/companion
/demo/cart           # V2 新增
/demo/product/1      # V2 新增
/demo/checkout       # V2 新增
/demo/orders         # V2 新增
```

---

## 附录

### A. 文件清单

**新增文件**:
- `/app/demo/cart/page.tsx`
- `/app/demo/product/[id]/page.tsx`
- `/app/demo/checkout/page.tsx`
- `/app/demo/orders/page.tsx`
- `/components/ui/*.tsx` (6个组件)
- `/components/business/*.tsx` (5个组件)
- `/contexts/*.tsx` (4个Context)
- `/hooks/*.ts` (4个Hooks)
- `/lib/types/cart.ts`
- `/lib/types/order.ts`
- `/lib/types/favorites.ts`
- `/styles/themes/*.css`

**修改文件**:
- `/app/demo/layout.tsx` (添加 Provider)
- `/app/demo/tutorials/[id]/page.tsx` (完善实现)
- `/components/layout/DemoSidebar.tsx` (添加新菜单)

### B. 依赖项

无需新增 npm 依赖，使用现有技术栈实现。

---

*V2 设计文档结束*
